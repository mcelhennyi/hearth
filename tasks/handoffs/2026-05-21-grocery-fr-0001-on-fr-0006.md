# Handoff — grocery-list FR-0001 on hearth FR-0006 (2026-05-21)

**Audience:** Next agent continuing design-language integration or FR-0002 Mantle UI.

## Executive summary

Implemented **grocery-list `FR-0001` MVP reference plugin** (all four tickets) on branch `feat/FR-0001-mvp-reference-plugin` @ `b4cc00b`, pushed to `github.com:mcelhennyi/grocery-list`. Hearth **`feat/FR-0006-design-language`** should bump `plugins/third-party/grocery-list` submodule to that commit. **FR-0002** Mantle UI in grocery-list remains **deferred** per owner.

## Delivered (grocery-list)

| Ticket | Summary |
|--------|---------|
| T-FR-0001-01 | `compose.yaml`, `./develop test`, 20 pytest, toggle 404 |
| T-FR-0001-02 | `groceries/spark.py` — pantry subscribe, empty-list `hub.notify.send` |
| T-FR-0001-03 | Spark `list.add` / `list.remove` / `list.items` capability dispatch |
| T-FR-0001-04 | `tests/smoke/e2e-hearth.sh` (manual against live hub) |

## Validation

```bash
cd plugins/third-party/grocery-list && ./develop test
```

## Suggested next step

1. Merge grocery-list PR `feat/FR-0001-mvp-reference-plugin` → `master` when reviewed.
2. On hearth `feat/FR-0006-design-language`, commit submodule bump and continue FR-0006 PR to `main`.
3. When ready for UI: start grocery **FR-0002** `T-FR-0002-01` (Vite + React + `@kindling/mantle`).

## Options

- Run `tests/smoke/e2e-hearth.sh` on Pi/local hub now to confirm live VAL for T-FR-0001-04.
- Open grocery-list PR only, or bundle with hearth FR-0006 integration PR.
- Pause FR-0002 until hearth FR-0006 merges to `main` (submodule + mantle package stable).
