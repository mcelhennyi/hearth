# T-FR-0004-13 — Hearth Users UI first admin and username login

**Branch:** `feat/FR-0004-centralized-users-auth-T-FR-0004-13-first-admin-login-ui`
**Worktree:** `.worktrees/FR-0004-centralized-users-auth/T-FR-0004-13-first-admin-login-ui/`
**Date:** 2026-05-26

## Summary

Replaced the remaining placeholder/static provider UI with a real Hearth Users form and made the server-rendered login/setup page prefix-aware when served through `/hearth-users/`. First-run setup collects username, display name, and password; existing installs collect username and password; JSON submissions preserve local-only `next` redirects and surface API error details for duplicate setup, disabled users, wrong passwords, and lockout.

## TEST

Added failing coverage before implementation for:

- `/hearth-users/login` setup HTML including username, display name, password, and JSON setup payload shape;
- existing-user login HTML including username/password and local-only `next` sanitization;
- static bundled UI no longer being an inert placeholder;
- clear API errors for wrong password, disabled accounts, and lockout.

RED result: `./develop test tests/builtin/test_hearth_users.py` failed on the missing prefixed endpoint wiring and the static placeholder UI.

## DEV

Implemented prefix-aware server HTML endpoints, added prefixed FastAPI routes for setup/login/logout/session/verify, and updated the bundled static UI (`web/index.html`, `web/src/main.ts`, `web/src/styles.css`) to submit the expected JSON shape with Mantle-aligned styling.

## VAL

- `./develop test tests/builtin/test_hearth_users.py` — 27 passed.
- `./develop test tests/builtin/test_hearth_users.py tests/api/test_auth_verify_alias.py tests/spark/test_broker.py` — 59 passed, 3 warnings.
- `./develop test` — 341 passed, 3 skipped, 4 warnings after initializing the `kindling` submodule in this worktree.
- `git diff --check` — passed.

Manual/Pi browser exception: no Pi or real browser target was available in this session; the automated HTML/API/static contract tests cover the setup/login payload and local-only redirect behavior.

## Next

Merge this ticket branch into `feat/FR-0004-centralized-users-auth` after review. `T-FR-0004-14` may proceed independently in its own worktree; `T-FR-0004-16` remains blocked until T13, T14, and T15 are complete.
