# T-FR-0004-02 — Built-in hearth-users plugin scaffold

## 2026-05-26 — TEST → DEV → VAL

**Branch:** `feat/FR-0004-centralized-users-auth-T-FR-0004-02-hearth-users-scaffold`

**Feature base:** merged `origin/feat/FR-0004-centralized-users-auth` at `5f461e4` before final validation. Conflict was limited to branch-local `CURRENT.md`.

**TEST:** Added RED coverage for the `hearth-users` manifest, first-boot built-in registration, API uninstall guard, plugin `/health`, and placeholder login UI.

**DEV:** Added `apps/builtin/hearth-users/`, `[plugin].builtin = true`, registry `builtin` persistence, first-boot built-in registration, uninstall protection, and dev Caddy/Compose routing at `/hearth-users/`.

**VAL:**

- `./develop test tests/tinder/test_loader.py::TestValidManifest::test_hearth_users_builtin_manifest_loads tests/api/test_builtins.py tests/builtin/test_hearth_users.py` — 5 passed.
- `./develop test` — 243 passed, 3 skipped.
- `./develop up -d`, then host `curl -k --resolve hearth.home.arpa:443:127.0.0.1 https://hearth.home.arpa/hearth-users/` — HTTP 200 placeholder login HTML.
- `curl -k --resolve hearth.home.arpa:443:127.0.0.1 https://hearth.home.arpa/hearth-users/health` — HTTP 200 `{"ok":true,"service":"hearth-users"}`.
- `./develop down` stopped the validation stack.

**Notes:** Host `curl` was used only to verify the exposed local Caddy route from outside Compose; tests and stack control used `./develop`.
