# T-FR-0004-14 — real user claims

Date: 2026-05-26
Branch: `feat/FR-0004-centralized-users-auth-T-FR-0004-14-real-user-claims`
Worktree: `.worktrees/FR-0004-centralized-users-auth/T-FR-0004-14-real-user-claims/`

## Summary

Completed TEST -> DEV -> VAL for `T-FR-0004-14`.

- Added cross-contract tests proving two distinct users keep distinct claims through `hearth-users` session/verify, Spark `session.current`, login/logout audit topics, hub `/api/auth/verify` signed headers, gateway-to-Kindling trust helpers, Mantle shell `hearth.user` postMessage, and `@kindling/mantle useUser()`.
- Fixed `@kindling/mantle` `UserInfo` so plugin code can receive `roles?: string[]` from `hearth.user`.
- Existing T12 runtime claim plumbing already returned real user ids/display names/roles for the backend paths; this ticket locks the behavior with broader contract coverage.

## RED

- `./develop web sh -lc 'corepack enable && cd /workspace && corepack pnpm --filter @kindling/mantle test'` failed as expected: `UserInfo type includes Hearth roles` did not find `roles?: string[]`.
- Backend and shell RED additions already passed on the T12 base, confirming those runtime paths were already mostly corrected before T14.

## Validation

- `./develop test tests/builtin/test_hearth_users.py tests/api/test_auth_verify_alias.py tests/api/test_gateway_identity_contract.py tests/test_kindling_plugin_contract.py tests/spark/test_broker.py tests/proxy/test_caddy.py` — 83 passed, 1 skipped, 8 warnings.
- `./develop web npm run test -- App.test.tsx` — 9 passed.
- `./develop web sh -lc 'corepack enable && cd /workspace && corepack pnpm --filter @kindling/mantle test'` — 38 passed.
- `./develop web npm run test` — 64 passed.
- `./develop web npm run build` — passed.
- `./develop web npx eslint src/App.test.tsx` — passed.
- `git diff --check` — passed.

## Known Blocker

- `./develop web npm run lint` remains blocked by pre-existing non-T14 lint errors in `src/dashboard/edit/*`, `src/shell/ChromeSlot.tsx`, `src/shell/useChromeSlotRegistry.ts`, `src/shell/usePluginFrameState.ts`, `src/shell/SettingsContext.tsx`, and `src/theme/ThemeProvider.tsx`.
