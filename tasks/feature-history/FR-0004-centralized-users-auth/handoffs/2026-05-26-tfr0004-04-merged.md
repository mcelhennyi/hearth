# T-FR-0004-04 merged — 2026-05-26

## Summary

`T-FR-0004-04` is merged into `feat/FR-0004-centralized-users-auth`.

Implemented:

- Hub `GET /api/auth/verify` alias.
- Nested auth provider settings (`auth.provider`, `auth.external_verify_url`).
- Fail-closed built-in/external provider behavior.
- Persisted `HEARTH_USER_SIG_SECRET` and signed `X-Hearth-User-*` response headers.
- Plugin Compose secret injection.
- Dev Compose dependency fix for `hearth-users`.

## Validation

- Focused merged slice — 75 passed.
- `./develop test` — 266 passed, 3 skipped, 3 warnings.
- Caddy sidecar probe: `/api/auth/verify` returned 401 without session and 200 with signed `X-Hearth-User-*` headers after login.

## Next

Next dependency-valid tickets:

- `T-FR-0004-05` — Caddy auth_request and header injection.
- `T-FR-0004-07` — Kindling template: trust middleware and no local login.
- `T-FR-0004-09` — External auth provider stub and operator settings UI.
