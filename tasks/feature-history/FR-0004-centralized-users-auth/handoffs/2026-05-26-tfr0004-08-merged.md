# T-FR-0004-08 merged

Date: 2026-05-26

Branch merged: `feat/FR-0004-centralized-users-auth-T-FR-0004-08-mantle-login-use-user`

Target: `feat/FR-0004-centralized-users-auth`

Summary:

- Mantle shell now fetches `/hearth-users/api/session` before rendering authenticated shell chrome.
- Unauthenticated users are sent to `/hearth-users/login?next=...`; the duplicate hub-local password form is gone.
- Plugin iframes receive verified `hearth.user` claims via postMessage.
- Service worker and install prompt behavior were adjusted for the authenticated shell flow.
- Kindling/Mantle compliance changelog notes the child-repo migration requirements.

Validation:

- `./develop web npm ci` — installed dependencies.
- `./develop web npm run test -- App.test.tsx` — 9 passed after merge-test fix.
- `./develop web npm run test` — 4 files passed, 12 tests passed.
- `./develop web npm run build` — passed.
- `./develop web npm run lint` — passed.

Manual exception:

- Real iPhone PWA login-once walkthrough was not available in this host environment; automated contract coverage verifies session fetch, login link, and plugin iframe payload.

Next:

- `T-FR-0004-10` capstone E2E.
