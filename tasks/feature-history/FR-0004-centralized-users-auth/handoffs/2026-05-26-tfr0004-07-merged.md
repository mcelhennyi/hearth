# T-FR-0004-07 merged

Date: 2026-05-26

Branch merged: `feat/FR-0004-centralized-users-auth-T-FR-0004-07-kindling-trust-template`

Target: `feat/FR-0004-centralized-users-auth`

Summary:

- Added `trust.py` to the Kindling Python plugin template with `require_hearth_user()` for gateway-injected identity headers.
- Updated generated `app.py` with a protected `/api/me` sample route.
- Updated the generated plugin README to make Hearth gateway identity authoritative and local login/session storage out of scope.
- Added `kindling/mantle/useUser.ts` and exported it for plugin UI guidance.
- Added `deploy/kindling-contract/COMPLIANCE_CHANGELOG.md` with dense child-repo migration notes for AI-assisted updates.

CI note:

- PR #51 amd64 install smoke failed before code checkout with GitHub HTTP 403 "account is suspended"; arm64 install smoke passed.
- The failure did not execute repository code, so the local combined-tree validation below is the merge gate.

Validation:

- `./develop test tests/test_kindling_plugin_contract.py` — 13 passed.
- `./develop test` — 275 passed, 3 skipped, 3 warnings.

Next:

- Staff `T-FR-0004-08` and `T-FR-0004-09`.
