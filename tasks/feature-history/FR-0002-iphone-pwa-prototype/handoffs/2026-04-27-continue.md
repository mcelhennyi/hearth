# 2026-04-27 — continue handoff (FR-0002)

**For the next worker** (human or agent). Self-contained — read this first, then dive into the specific files referenced.

## Where we are right now

- Repo is initialized as **Hearth**. `main` is at HEAD on `origin = git@github.com:mcelhennyi/hearth.git`. Two commits in this session:
  - `6c26620` — bootstrap Hearth: identity, design surface, FR-0001 ticket plan.
  - `49827e7` — `/plugin-idea` skill + `native-plugin-ideas.md` backlog + `plugin-ideas/system-backup.md`.
- **FR-0001 (Hearth platform MVP)** is `parked`. Its design (`README.md` + `10-design/` + `tickets.md` + `20-tickets-dag.md`) and the shared `docs/design/` surface (architecture, plugin-contract, spark-api, mantle-ui, deployment, notifications, roadmap, satellite-repos/{kindling,ember}) are all written and authoritative. **Tickets `T-FR-0001-01..10` are unchanged but should not start until FR-0002 closes.**
- **FR-0002 (iPhone PWA prototype)** is the active feature. It exists in `tasks/feature-history/FR-0002-iphone-pwa-prototype/` with `README.md`, `10-design/{charter,risks}.md`, `20-tickets-dag.md`, `tickets.md`, `serial-diary.md`. Its job is to de-risk the iPhone-PWA story end-to-end before FR-0001 implementation starts.
- The FR-0002 stub is **uncommitted on disk at the moment this handoff is being written**; the next thing my own session will do (or the next worker, if I stop here) is the FR-allocation commit + push so the FR-0002 reservation deconflicts.

## What FR-0002 is, in two sentences

A vertical slice that proves the iPhone PWA story works on real hardware: Caddy with `tls internal` serving `https://hearth.local/`, a static Mantle shell with manifest + service worker + bottom-tab nav, the iPhone CA-trust workflow executed on a real iPhone, and one VAPID-signed Web Push round-trip from a tiny FastAPI service to that PWA. **No** plugin loader, **no** Tinder, **no** Spark, **no** Kindling repo split — those are FR-0001.

The five risks are listed with explicit fail triggers in [`../10-design/risks.md`](../10-design/risks.md). The acceptance demo (five steps on a real iPhone) is at the bottom of [`../README.md`](../README.md).

## Where to start

1. **Read** in this order: `tasks/feature-history/FR-0002-iphone-pwa-prototype/README.md` → `10-design/charter.md` → `10-design/risks.md` → `tickets.md`. Skim `docs/design/mantle-ui.md`, `deployment.md`, `notifications.md` because the prototype tests their assumptions.
2. **Set up the worktree**: `git worktree add -b feat/FR-0002-iphone-pwa-prototype .worktrees/FR-0002-iphone-pwa-prototype/feature/`. Then for each ticket, branch a child off the feature branch as documented in `docs/ai-context.md` § Worktree policy.
3. **Run `/identify-frontier`** to confirm the eligible set (it should be `T-FR-0002-01` and `T-FR-0002-02` in parallel). Then `/develop-frontier` if you want subagents per ticket, or do them serially yourself.
4. **Start `T-FR-0002-01`** (Caddy + tls internal + static placeholder) **and/or `T-FR-0002-02`** (Mantle PWA bones) — they share no code. `T-FR-0002-03` (Web Push) needs both.

The ticket `tickets.md` has **concise** TEST/DEV/VAL exit criteria — flesh them out before starting work and record any expansion in `serial-diary.md`. Specific TODOs per ticket:

- **`T-FR-0002-01`**: confirm Compose works on the dev host; document mDNS or `/etc/hosts` for Linux dev hosts that don't run Avahi. Ship `./develop ca-export` end-to-end.
- **`T-FR-0002-02`**: Tailwind + shadcn/ui + Vite-PWA + Apple meta tags. App icons from `docs/design/logo.svg`. Bottom-tab nav placeholder with 4 tabs that all render the same "PWA-ready" tile. Theme tokens from `docs/design/mantle-ui.md` § Theme tokens.
- **`T-FR-0002-03`**: VAPID keypair at `var/hearth/secrets/vapid.{pub,priv}` (same path FR-0001 uses). Subscriptions persisted to `var/hearth/push-subscriptions.json` (no SQLite — keep it tiny). Note any auth shortcut in the diary so it doesn't leak into FR-0001's `T-FR-0001-09`.
- **`T-FR-0002-04`**: do not skimp on the report. Each R1–R5 risk in `10-design/risks.md` needs an explicit verdict and (if it fails) a linked amendment commit against `docs/design/...`.

## What's intentionally not done in this handoff

These would clutter the FR-0002 stub if I forced them in. Pick them up when relevant:

- **No** worktree created yet. The next worker creates it.
- **No** FR-0001 amendment yet. The FR-0001 design stays the source of truth until FR-0002 finds something that contradicts it.
- **No** `40-prototype-report.md` yet — that file gets created at FR-0002 closeout.
- **No** plugin scaffolding (no Kindling, no `apps/<slug>/`). FR-0002 produces a static shell at `apps/hub/web/` and a tiny `apps/hub/api/` only.
- **CLAUDE.md is touched by linter** — note that CLAUDE.md is a `sync-skeleton` target, so its `/plugin-idea` row will be overwritten on the next sync unless we contribute it upstream first via `./push-skeleton contribute`. Decision deferred.

## Open questions (live)

- **FR-0001 Q4** (auth default): unresolved. FR-0002 ducks it with a single hard-coded session cookie or no auth on a LAN-only host. Decide before `T-FR-0001-09`.
- **FR-0001 Q11** (VAPID rotation policy): unresolved. FR-0002 reuses the same `var/hearth/secrets/vapid.{pub,priv}` path so the prototype keypair survives into MVP. Whether rotation invalidates subscriptions or migrates them is FR-0001's call.
- **FR-0002 P5** (CA-trust UX fallback): only relevant if R1/R2 fail in the prototype. If they do, raise a follow-up question against FR-0001 about Tailscale-issued certs vs DNS-01 Let's Encrypt.

## Risks I'm aware of but didn't act on

- The `/plugin-idea` linter commit (`49827e7`) edited `CLAUDE.md`, which is a skeleton-sync target. On next `./sync-skeleton` it will be overwritten unless contributed upstream. Mentioned in the diary; not blocking FR-0002.
- `develop` mode bit changed (executable) during init-skeleton; benign but noted in the first commit.
- No CI configured yet. A CI stub belongs in `T-FR-0001-01`, but a tiny GitHub Actions check (lint, type-check) for FR-0002 would catch regressions sooner. Optional — flagging for the next worker.

## Useful commands

```bash
# Where to be
cd /Users/ianmcelhenny/projects/project_04

# Inspect state
git log --oneline -5
git remote -v
cat tasks/ticket-progress.md

# Read FR-0002
cat tasks/feature-history/FR-0002-iphone-pwa-prototype/README.md
cat tasks/feature-history/FR-0002-iphone-pwa-prototype/10-design/risks.md
cat tasks/feature-history/FR-0002-iphone-pwa-prototype/tickets.md

# Start work
git worktree add -b feat/FR-0002-iphone-pwa-prototype \
  .worktrees/FR-0002-iphone-pwa-prototype/feature/

# Pick eligible tickets
/identify-frontier   # in Claude Code
/develop-frontier    # one subagent per ticket
```

## Session status at handoff

`handoff` (per `tasks/ticket-progress.md`). When work resumes, flip to `developing` and update **Active ticket** + **Branch / worktree**.
