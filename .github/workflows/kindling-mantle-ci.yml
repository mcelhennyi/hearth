# @kindling/mantle — build, test, and package dry-run (T-FR-0006-15 TEST/VAL).
name: kindling-mantle CI

on:
  pull_request:
    paths:
      - "packages/mantle/**"
      - "pnpm-lock.yaml"
      - ".github/workflows/kindling-mantle*.yml"
  push:
    branches:
      - main
      - "feat/**"
    paths:
      - "packages/mantle/**"
      - "pnpm-lock.yaml"
      - ".github/workflows/kindling-mantle*.yml"

concurrency:
  group: kindling-mantle-ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  validate:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: packages/mantle
    steps:
      - uses: actions/checkout@v4

      - uses: pnpm/action-setup@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: pnpm
          cache-dependency-path: pnpm-lock.yaml

      - name: Install workspace dependencies
        run: pnpm install --frozen-lockfile
        working-directory: .

      - name: Test (typecheck, build, vitest)
        run: pnpm run test

      - name: Package dry-run
        run: npm pack --dry-run --ignore-scripts
