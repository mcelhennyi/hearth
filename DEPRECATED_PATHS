# Machine-readable list of paths to REMOVE from the *consumer project root*
# when running ./sync-skeleton (after git pull in .skeleton/).
#
# Rules:
# - One path per line, relative to the consumer repo root (NOT prefixed with .skeleton/).
# - No ".." segments; no absolute paths.
# - Lines starting with # are ignored.
# - Globs are NOT supported; list concrete paths (add multiple lines if needed).
#
# Example (commented):
# legacy/old-rule.mdc
