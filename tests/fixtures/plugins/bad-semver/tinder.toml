[plugin]
slug       = "bad-semver"
name       = "Bad semver"
version    = "not-a-version"
hearth_min = "0.1.0"
kind       = "app"

[entrypoint]
backend = { kind = "python", module = "bad.app:create_app", port_env = "HEARTH_PLUGIN_PORT" }
ui      = { kind = "static", path = "web/dist" }
