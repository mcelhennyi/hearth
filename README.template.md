# {{PROJECT_NAME}}

{{ONE_LINE_CHARTER}}

## Documentation

Use this section for the project's source-of-truth documentation:

- Product and system design:
- API or user docs:
- AI workflow notes: [`docs/ai-context.md`](docs/ai-context.md)

## Development

Add build, test, and run commands after the stack is chosen.

If the repository uses the skeleton `develop` helper, start with:

```bash
./develop help
```

## Project Skeleton

This repository was initialized from the project skeleton. Template sources live in `.skeleton/`; run `./sync-skeleton` when you intentionally want upstream process/tooling updates.

The project `README.md` is owned by the project. Regenerate or edit it from this template during project initialization; do not treat the skeleton README as a sync target.

## License

{{LICENSE_OR_VISIBILITY_NOTE}}
